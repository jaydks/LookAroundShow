<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LookAroundShow"
        tools:targetApi="31">
        <activity
            android:name=".BookActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.MyActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.LoginActivity"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presenter.BookActivity"
            android:exported="false" />
        <activity
            android:name=".presenter.ShowDetailActivity"
            android:exported="false"
            android:label="@string/title_activity_show_detail"
            android:theme="@style/Theme.LookAroundShow" />
        <activity
            android:name=".presenter.MainActivity"
            android:exported="true" />
    </application>

</manifest>